# Q1-T04 result

**Corrected result:** **INSUFFICIENT / NOT REPRODUCIBLE.** This correction
supersedes the original PASS summary.

`no-auto-update-license-scan.log` records package scripts, excluded manual update
paths, selected Bun/Turbo source lines, a claimed no-match source-token scan, and
the copied-material list. It does not retain the exact unelided commands, scan
patterns, environment/profile, timestamp, or explicit exits used to produce the
aggregate receipt. It is therefore a set of static observations, not a
reproducible no-auto-update, copied-material, or network result.

The receipt identifies a Bun npm-wrapper `postinstall`, while the recorded
candidate is a release binary selected by digest. It also records `bun upgrade`
as outside the command allowlist. Those observations can inform a future exact
rerun but do not establish a qualified control. Raw bytes are preserved and
hashed in `SHA256SUMS`; see the
[evidence sufficiency ledger](../EVIDENCE-SUFFICIENCY.md).
