# ADR 0036: M6 owned synthetic cell D7

**Status:** Accepted 2026-08-18 for one repository-only cell

The project owner accepts D7 only for `en-US / US / project-authored fictional
observatory technical-support docs`, fixture `fixtures/m6-owned/v1.0.0`, and its
eight manifest-digested documents. The owner dedicates under CC0 only rights
they control. AI assistance is disclosed; provider training data, exclusivity,
originality, and uncontrolled rights remain unclaimed.

Fetched bytes remain provisional in memory until manifest identity, digest,
media, UTF-8, robots, URL policy, and the injected synthetic safeguard all
clear. Missing, false, or throwing safeguard behavior persists zero capture
bytes; this plumbing makes no efficacy claim. Successful capture, document,
snapshot, and citation lineage is immutable and remains an inactive candidate.
Lexical-v1 exact-token scoring is deterministic with document-ID tie breaking
and an explicit no-answer. Tombstones dominate query, rebuild, deletion, and
restored bytes, preventing resurrection.

The fixture has controlled failure routes and a local project-CA key used only
for tests. No real person, publisher, public site, production identifier,
rendering, vector, learned ranker, multitenancy, or expansion cell is accepted.
