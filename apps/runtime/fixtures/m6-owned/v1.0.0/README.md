# M6 repository-only owned cell v1.0.0

Exactly eight project-authored fictional observatory technical-support documents
for `en-US / US`, served only by the local project-CA TLS fixture at logical
origin `https://docs.m6-owned.test`. No public DNS or egress is authorized.

The requester, as project owner/rightsholder, dedicates to CC0 only rights they
control in these fixture bytes. AI assistance was used; no representation is
made about provider training data, output exclusivity, originality, or rights
beyond those controlled by the requester. The fixture is synthetic test data,
not operational advice and not evidence that any safeguard is effective.

The PEM CA and server keys under `tls/` are intentionally committed fixture-only
credentials for loopback tests. They trust only `docs.m6-owned.test`, are not
used by production, and must never be installed in a user or system trust store.
`qrels.json` is the immutable six-development/six-held-out lexical judgment set.
