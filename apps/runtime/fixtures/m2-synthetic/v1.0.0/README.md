# M2 synthetic fixture v1.0.0

Immutable, fictional, AI-authored test bytes. It contains no intentional real
personal data, credentials, source excerpts, or production claims. The manifest
records exact reserved `.invalid` HTTPS source URLs; they are synthetic
provenance, not live services or local filesystem paths. The manifest
`sha256` is reproducible as SHA-256 over sorted lines of
`path + NUL + document-sha256 + LF`. Withdrawal is requested through the project
owner; on approval, remove query visibility immediately, retain a non-content
tombstone, remove objects/projections, and propagate to repository copies at the
next controlled update. Repository history and backups follow repository policy
and must not be represented as immediately erasable.
