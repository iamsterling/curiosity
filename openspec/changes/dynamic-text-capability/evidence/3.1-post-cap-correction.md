# Task 3.1 post-cap critical correction

Date: 2026-08-16

Decision: **BLOCKED / FAIL-CLOSED.** Task 3.1 is unchecked; task 3.6 remains blocked; no font bytes were executed. No third independent review occurred.

The capsule now uses version-3 expected-value metadata without trusted isolation booleans. Live admission validates loopback-only interfaces/routes, the exact mount and device inventory, read-only root and cgroup-v2 mounts, bounded allowlisted tmpfs, exact UID/GID/capabilities/no-new-privileges/environment/cgroup limits, and fails as `HARNESS_ADVERSARIAL_LIVE_ISOLATION_MISMATCH` before adversarial launch.

The pinned live negative matrix rejects `--network=bridge`, writable root, extra tmpfs, extra read-only bind, and an extra `/dev/zero` device mapping. The writable-cgroup mount-option branch has an exact parser negative; a live writable-cgroup container was not created because that requires privileged or host-cgroup mounting, both prohibited non-goals.

The watchdog treats `docker rm --force` failure as fatal and separately verifies absence. Normal, timeout, SIGINT, SIGTERM, controller-loss, and container-error paths retain `{ attempted: true, commandSucceeded: true, absent: true }` against the exact container ID. SIGINT/SIGTERM controls signal only the provider test subprocess itself.

Residual blocker: Docker Desktop with `--network=none` still exposes non-loopback tunnel interfaces in `/proc/net/dev`. Strict nominal adversarial admission therefore rejects. Raw retained output is in `3.1-post-cap-build.txt`, `3.1-post-cap-negative-matrix.txt`, `3.1-post-cap-abnormal-cleanup.txt`, `3.1-post-cap-benign.txt`, `3.1-post-cap-admission-blocked.txt`, and `3.1-post-cap-verification.txt`.
