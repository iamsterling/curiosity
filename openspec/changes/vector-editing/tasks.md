# Vector editing — tasks

## 1. Kernel: point types

- [x] 1.1 `document.ts`: `handleMode` gains `"auto"`; validation — auto points store no handles (`VECTOR_POINT_AUTO_HANDLES`), single-point subpaths refuse non-corner modes (`VECTOR_POINT_MODE_UNSUPPORTED`) <!-- the min-2 subpath rule makes single-point subpaths unrepresentable, so `VECTOR_POINT_MODE_UNSUPPORTED` is unreachable — the auto derivation handles 1- and 2-neighbour cases; the code stays out of the dead path -->
- [x] 1.2 `commands.ts`: `set-point-type` with the full conversion matrix (corner↔free↔asymmetric↔mirrored↔auto, per-pair semantics from design.md) — deterministic, exact inverse carrying the pre-conversion record, `changed` honesty
- [x] 1.3 `path-geometry.ts`: `deriveAutoHandle` + `convertPointType` — the closed chord-tangent formula (both/single neighbours, ⅓ lengths), and `resolveAutoHandles` — the resolved-geometry projection (disposable; materializes auto as `asymmetric` so the renderer packet vocabulary stays closed)
- [x] 1.4 Tests: the matrix (every pair: determinism, inverse, corner semantics); auto fixtures; validation codes; canonical serialization round-trip of `"auto"` (`vector-point-types.test.ts`, 16 tests)

## 2. Kernel: pen/node tool effects

- [x] 2.1 `interaction.ts`: the closed effect vocabulary — pen (begin/add-point/preview/close/join/end) and node (select-points, marquee, move-points, move-handle, insert-point, cycle-type); the modifier grammar applied in the reducer (shift=link, alt=preserve-length, control=45° — `constrainHandleDrag` in path-geometry), never in DOM handlers; path-level hit tests (`hitTestPathPoint`/`Handle`/`Segment`/`Endpoint`) on the RESOLVED geometry, screen-constant tolerance ÷zoom
- [x] 2.2 The demote-on-edit: dragging an auto point's handle materializes the resolved handles as authored `asymmetric` in the same preview dispatch (single `set-path-points` with the rebased record set — the inverse restores the auto record, so the demote is invertible)
- [x] 2.3 Tests (harness, no DOM): click-add, click-drag handles, insert-on-segment (double-click, exact split), cycle-type, close/join, marquee point selection — one transaction per gesture, cancel rolls back completely (`harness.test.ts` pen/node block, 5 tests) <!-- plus reducer-level tests in `path-selection.test.ts` -->

## 3. App wiring

- [x] 3.1 `harness.ts`: pen/node effects land in `applyEffect`; the pen session (one transaction, one history entry, rebased pinned geometry with placement in bounds); `endPenSession`/`hasPenSession`; the scene adapter no longer throws on path nodes (path layers project invisible — the glass-records pattern; the real geometry must still reach the renderer via the packet channel: **remaining** — the path-command composition in `webgpu-renderer.ts`/`wasm-bridge.ts` is a follow-up task) <!-- tail done: `pathCommands` on the projection (memoized with the sceneCache, resolved geometry + world transform + visible-slot order); `SceneRenderer.render` options gains `pathCommands`/`overlayCommands`; wasm-bridge forwards them; webgpu-renderer `withPathCommands` appends to the authored list (module re-sorts by (zIndex, order)) and `withOverlays` folds `overlayCommands` above the preview. The world walk composes translate(bounds.x/y) × node.transform down the tree — the interaction.ts `visiblePathNodes` pattern, NOT projectGlassRecords (whose draw rect is the bounds; path points are local, so the placement must ride the transform). -->
- [x] 3.2 `canvas-stage.tsx`: editing overlays — grippies (screen-constant ÷zoom radii), rubber band, close indicator, in-progress pen segment — host-composed renderer state, never authored geometry <!-- done: `editing-overlays.ts` (the pageOverlay precedent — separate module, composed in canvas-stage's rAF loop) emits plain rect DrawCommands per frame from the projection: node tool = grippy squares + handle lines/end squares + point marquee (fill + outline, the preview colors; draftBounds now mirrored on the projection by the node-marquee effect, withheld from the select-tool preview channel); pen tool = session segments, first-point handle, pending segment + handle, close ring at the first point (square ring — rect-only composition, the 4-rect outline pattern). `selectedPointGrippies`/`penSessionWorld`/`penPreviewWorld` are world-coordinate projections; pen-begin now clears the stale penPreview like pen-add-point. The fold-in re-bases (zIndex, order) so overlays draw above the preview block. -->
- [x] 3.3 `editor-toolbar.tsx` via `layout.tsx`: the pen and node tools join the toggle group; Escape ends the pen session in `keyboard-bindings.tsx` (behind the text-entry gate)
- [x] 3.4 Tests: harness-level tool flow (draw a path with the pen, edit with the node tool, undo restores the document exactly)

## 4. Kernel: the boolean engine

- [x] 4.1 `path-geometry.ts`: cubic-cubic intersections (recursive clipping, epsilon), dedup + sort; `flattenCubicChain` gains backdata (`{subpathId, segmentIndex, tStart, tEnd}`) <!-- `cubicCubicIntersections`: recursive de Casteljau clipping with a work budget (8192 nodes), depth cap, bbox-pruning BEFORE the fallbacks, and a closed-form linear-linear solver for near-linear pairs (exact collinear-overlap endpoints; hits are mapped from the sub-curve's local parameter space back onto the original segments). `flattenCubicChain` was refactored to `flattenCubicChainWithBackdata`/`flattenSubpathWithBackdata`; `flattenSubpath` and `pointInSubpath` are now thin projections over it — one flattening implementation. **FOLLOW-UP FIX (the T-junction family — a section-4 follow-up): the closed-form solver returns CHORD FRACTIONS (a linear parametrization), but the split step evaluates the CUBIC parameter — for a degenerate-linear segment (what corner paths produce) the parametrization is 3t² − 2t³, so only fractions in {0, 0.5, 1} coincide. A crossing at the geometric quarter of an edge landed at the cubic parameter where the curve is only an eighth of the way along; the two operands' cuts never met, the topology grid never connected, and the engine resolved empty for every non-midpoint crossing (the midpoint/shared-edge/contained/corner-touch families survived exactly because their crossings sit at the fixed points). The remap now inverts the chord fraction to the true cubic parameter per interval — `cubicTAtFraction`: S⁻¹(S(t0) + s·(S(t1) − S(t0))) with S(t) = 3t² − 2t³, Newton-solved (exact at the fixed points, so the existing midpoint fixtures round-trip byte-identically; the de Casteljau split preserves the parameter, so the interval-relative inversion is exact for degenerate sub-curves too) -->
- [x] 4.2 The sweepline combine (Bentley–Ottmann over flattened edges, quantized topology grid, winding numbers vs per-operand fill rule, edge selection per operation) <!-- split-then-classify instead of a sweepline (the measured optimisation, not the v1): every segment is split at every pairwise intersection parameter; each fragment is classified by the selected region's status on its two sides (samples one grid unit off the edge, quantized to the 1/512-of-extent topology grid); the surviving boundary is traced with the region on the left (first kept fragment COUNTERCLOCKWISE from the incoming direction — the region stays on the left of both edges; a clockwise scan traces contours that enclose non-region pockets, as the exclude fixture shows). `windingNumberAt` is the exported per-subpath directional ray cast (+1 upward / −1 downward); per-operand fill rule honored (nonzero: Σ winding ≠ 0, even-odd: parity). Edge selection: union = any operand inside; intersect = EVERY operand inside (equals the pairwise "≥ 2" for two operands, correct for n); subtract = first inside ∧ later operands outside; exclude = odd total. Coincident boundary pieces from different operands are deduped before tracing (identical quantized endpoints, region on the same world side) -->
- [x] 4.3 Re-emission: surviving edges' backdata re-emits original curve fragments between cuts — results keep bezier pieces <!-- each maximal run of contiguous surviving fragments from the same original segment merges back into ONE cubic fragment between the run's extreme cut parameters (de Casteljau re-parameterization; the merged t-range is re-emitted at emission time, not the first fragment's); straight fragments (control points collinear with the chord — the split of a degenerate linear cubic has control points off the endpoints, so handle-delta tests fail) become corner points; curved fragments carry `free` handles. Fresh point/subpath ids under the caller's prefix, order keys `orderKeyForSigned(i * ORDER_KEY_STEP)`, result rebased so the bbox min corner is (0,0); contours are traced with the region consistently on one side, so holes are opposite-oriented subpaths and the result fill rule is nonzero -->
- [x] 4.4 Commands `union`/`intersect`/`subtract`/`exclude` with the precondition gates (`VECTOR_BOOLEAN_MIN_OPERANDS`, `VECTOR_BOOLEAN_OPEN_SUBPATH`, `VECTOR_BOOLEAN_NO_AREA`) — failed preconditions leave the document unchanged <!-- `boolean-operate` (nodeIds + operation) is a destructive join: validates preconditions (engine-side, thrown codes), computes the merged geometry from the operands' RESOLVED geometry (pinned geometry + bounds placement), creates ONE new path node (fresh deterministic id `boolean-N`, placement = the result's world-bbox min corner, fill + zIndex from the topmost operand in child order), deletes the operands. The inverse `restore-boolean-operands` carries the original operand records byte-exactly (in their original child order) + the result nodeId + parentId/index + the operation; its own inverse is the forward command with the same payload (the result node is recomputed with a fresh id on redo). Both go through `assertValid`. Mixed-parent operands are refused (`VECTOR_BOOLEAN_PARENTS_DIFFER` — an added code for an unlisted precondition; the single-parent inverse cannot represent them) -->
- [x] 4.5 Tests: fixtures with known results for all four ops; precondition codes; fragment re-emission asserted (segment counts + cut parameters); robustness cases (near-degenerate overlaps, self-intersections) <!-- `path-boolean.test.ts`, 34 tests (27 original + 7 T-junction regression): the four ops on overlapping rectangles (union = the 8-point L-outline, intersect = the 50×50 overlap, subtract = the 6-point notch, exclude = the two 6-point L-lobes — traced separately since the lobes only touch at the pinch corners), shared-edge adjacency (6-point 2×1 outline), corner-touch (two 4-point loops — the correct region boundary, unlike the figure-eight a clockwise scan would trace), circle ∪ interior-rect re-emits the circle (positions/modes exact, handles within float round-trip), circle ∪ partial rect keeps 2 exact corners + 5 curved runs with the cut at the true crossing, all three precondition codes at engine and command level (document unchanged on failure), nonzero-vs-evenodd (a same-orientation hole is a hole under even-odd and filled under nonzero; a real nonzero hole respects the hole), winding-number primitives, intersection units (crossing, parallel, collinear overlap endpoints, touching point, curve-line), determinism (byte-identical on re-run), command round-trips (undo restores the operands byte-exactly via `serialize()`, redo recomputes, one history entry). **WIDENED MATRIX (the T-junction follow-up)**: the offset-squares family — two 100×100 squares offset (25,25), every crossing at t = ¼/¾ (union = the 8-point L-outline, intersect = the 75×75 overlap at placement (25,25), subtract = the 6-point L, exclude = the two 6-point L-lobes, determinism byte-identical; crossing coordinates carry sub-1e-13 float noise from the parameter inversion, asserted within 1e-9); the corner-on-edge T — a 75×25 square whose corner lands on the other's edge with a coincident run (union = the square's own 4-point outline, intersect = the inner rect, subtract/exclude = the 8-point notch — the corner dedupe merges the coincident edge pieces); a curved-vs-straight mid-edge crossing — circle ∪ 70×45 rect at (35,25), crossings at x = 50+√1875 and x = 50+√2100 (t = 0.83/0.87 on the rect edges, mid-arc on the circle; asserted within 0.05, the near-linear closed-form tolerance; 2 exact rect corners) -->

## 5. Kernel: the compound

- [x] 5.1 `document.ts`: the `compound` node kind (`operation`, ordered `memberIds` via `childIds`); validation (shape-producing members, ≥2, acyclic) <!-- `compound?: { operation }` on `DocumentNode` — the members ARE the ordered `childIds` (subtract/exclude read the order), so the record carries only the operation. `compound` joined `NODE_KINDS_V3` additively (new values only appear when authored — no schema bump, no migration; a v2 reader rejects compound documents at its own kind set, exactly like `path`). Fine codes ride the coarse `DOCUMENT_INVALID` message (the established pattern): `COMPOUND_OPERATION_INVALID`, `COMPOUND_MIN_MEMBERS` in `validateNode`; `COMPOUND_MEMBER_KIND_UNSUPPORTED:<childId>` in a structure-validator pass (needs the other nodes). Member-kind restriction (v1): rectangle/frame/path ONLY — no nested compounds, no group/text/image/page-root, because the outline projection consumes member geometry directly with no nested resolution (a group member would silently drop its subtree from the outline). The bounds are DERIVED (auto-fit — Figma's published behavior) and recomputed by the commands; validation stays shape-only (bounds are not re-verified against the computed outline — the boolean computation is too heavy and precondition-sensitive for the coarse validator, and commands enforce the derived-bounds discipline at every write) -->
- [x] 5.2 Commands: `create-compound`, `set-compound-op`, `reorder-compound-member`, `flatten-compound` (destructive bake → path node; `VECTOR_FLATTEN_UNREPRESENTABLE` for holes/disjoint contours) <!-- All four + two restores through `assertValid`, single history entries, no bounds carried in any inverse (undo/redo re-derive from the restored members — derived state). `create-compound { nodeId, parentId, index, memberIds, operation }` reparents the members under the new node, inherits the topmost member's surface (fill/stroke/zIndex — the boolean precedent), computes the outline once for bounds; inverse `restore-compound-members { compoundNodeId, members (byte-exact pre-reparent records, original child order), parentId, index, operation }` — its own inverse is the forward command with the same payload. `set-compound-op` inverse carries the previous operation (`changed` honesty — same-op is a no-op with no history entry). `reorder-compound-member` is a REAL semantic command (subtract/exclude read order): the outline resolves from the REORDERED member order and the bounds re-derive from it (union/intersect recompute to the same value); inverse swaps the indices back. `flatten-compound` bakes the outline into ONE path node named after the compound (inheriting its surface); the whole member SUBTREES go with the bake; inverse `restore-flattened-compound { flattenedNodeId, compoundNodeId, nodes (the full removed set — compound + member subtrees, byte-exact), parentId, index }`; its own inverse is the forward flatten (fresh minted `flatten-N` id on redo). **The `VECTOR_FLATTEN_UNREPRESENTABLE` interpretation**: the Sketch-derived warning is about a model that cannot hold multi-subpath results — Crafty's path model CAN (holes and disjoint contours are ordinary subpaths), so the code fires it ONLY when the outline cannot be produced at all: the engine's `VECTOR_BOOLEAN_*` precondition codes surface as-is, and a produced-but-EMPTY result (disjoint intersect, full-cover subtract, identical exclude — no subpaths at all) is the unrepresentable case. The same empty-aggregate at create/set-op/reorder is refused with the engine's own `VECTOR_BOOLEAN_NO_AREA` (the derived-bounds model cannot hold a zero-area outline; interpreted and annotated here). Added codes for unlisted preconditions (the boolean `VECTOR_BOOLEAN_PARENTS_DIFFER` precedent): `COMPOUND_MEMBERS_PARENTS_DIFFER` (mixed-parent members — the single-parent inverse cannot represent them) and `COMPOUND_MEMBER_INDEX_INVALID` (out-of-range reorder). KNOWN LIMITATION surfaced during verification: the section-4 engine resolves empty for generic T-junction overlaps (crossings at non-midpoint parameters — e.g. two 100×100 squares offset (25,25)); its covered envelope is the midpoint/shared-edge/contained/corner-touch families. Compounds report those as an unproducible outline (projection draws nothing, commands refuse loudly with `VECTOR_BOOLEAN_NO_AREA`); the engine fix is a section-4 follow-up, deliberately not touched here -->
- [x] 5.3 `resolveCompoundOutline`: the resolved projection (disposable) consumed by the scene projection and hit testing; re-resolves on member edit <!-- `compound.ts` (sibling of `boolean.ts`): `compoundMemberOperand` (path members via `resolveAutoHandles` — the engine consumes path geometry and auto stores no handles; rectangle/frame members contribute their tight rect as a pinned 4-point path — the authored `cornerRadius` is NOT yet carried into the outline, a v1 limitation annotated here), `compoundMemberOperands`, `resolveCompoundOutlineResult` (geometry + derived world placement) and `resolveCompoundOutline`. Pure, deterministic across calls, never written back; the projection-facing forms never throw (unresolvable members, engine preconditions or an empty result → `undefined`; the commands resolve strictly so the codes surface loudly). `scene-adapter.ts`: `layerTypeFor("compound")` → "rectangle", and compound layers + every member-subtree layer project with opacity 0 (threaded `invisible` flag in `layerFromNode`) — the outline is the only visual, the layers stay for hierarchy, selection and the spatial index. Harness `pathCommands`: the walk emits the compound's outline as a path `DrawCommand` (resolved outline, `(zIndex, order)` from the same walk) and skips the members' own paths; the world transform composes the outline's DERIVED placement × node.transform — the authored compound bounds are NEVER trusted (a member edit is not a compound command, so they can be stale; the emission always re-resolves). Members stay individually editable (the node tool's point walk still reaches path members; the outline re-resolves on the next call) -->
- [x] 5.4 Tests: round-trip, validation codes, outline re-resolution, flatten diagnostics, clipboard carry <!-- `path-compound.test.ts`, 21 tests: create-compound round-trip + inverse (serialize byte-exact) + raw-machinery forward/inverse/redo; validation codes at command and document level (operation, min members, member kind, mixed parents, index); set-op bounds re-derivation + inverse + changed honesty (no-op adds no history entry); reorder flips the subtract outline (bounds 0,0→50,50) + inverse + no-op; outline re-resolution on a member `set-bounds` (projections differ, derived bbox asserted, authored bounds shown stale); determinism (byte-identical across calls); flatten → the known 8-point L-outline + byte-exact undo + redo; the multi-subpath exclude flatten (2 contours, 12 points — fully representable, no diagnostic); `VECTOR_FLATTEN_UNREPRESENTABLE` on the empty subtract (valid document, direct manipulation — uncreatable via commands); engine precondition codes surface as-is (open-subpath member; the projection returns undefined on the same document); member-subtree bake + byte-exact restore; clipboard carry (record + children through copy/serialize/parse/paste, minted members re-parented). Harness (`harness.test.ts` "compounds through the harness" block, 2 tests): create via dispatch → `pathCommands` carries exactly the outline command (8-point L, topmost zIndex, derived bounds) and none of the members'; the outline command re-resolves on a member `set-bounds` with the derived rect winning over the stale authored bounds; scene layers for the compound AND its members project opacity 0 -->

## 6. Docs, ADR and close-out

- [x] 6.1 ADR 0014: the vector-editing substrate — conversion matrix + auto intent (adopted from the research, demote-on-edit deliberate), the boolean engine decision (own kernel-side Inkscape-style pipeline; licensed sweep libraries and WASM placement rejected with flip triggers), the compound as authored state / outline as resolved value, the rejected alternatives cited to `docs/research/vector-editing.md` <!-- `docs/architecture/adrs/0014-vector-editing.md` (0013 is reserved by the in-flight authored-layout change). Records honestly: corner = no-handle state (invertible at the command level, never lossless — a deliberate deviation from FontForge's corner-keeps-handles); split-then-classify as v1 with Bentley–Ottmann recorded as the measured optimisation; the 1/512-of-extent topology grid with the parallel-edge envelope on record ("features within one grid cell of a parallel edge are unstable by design"); the T-junction defect (chord-fraction vs cubic-parameter on degenerate-linear segments) and the `cubicTAtFraction` fix; the `VECTOR_FLATTEN_UNREPRESENTABLE` interpretation (multi-subpath results ARE representable — fires only for unproducible/empty outlines); the host-composed `pathCommands`/`overlayCommands` render channel and the `SCENE_ADAPTER_UNSUPPORTED_KIND:path` crash it fixed (protocol stays v4); the honest caveats (square-ring close indicator, shape-only compound members, member opacity-0 projection, silent save/reload degradation — the finding-5 change of character). Alternatives cited to the research: index/positional identity, XML-diff undo, geometry-only inference, QPainterPath/paper.js wraps, licensed sweep libraries, WASM placement, the Figma network deferred with its published costs, sweepline-as-v1 deferred. -->
- [x] 6.2 `current-state.md` reality updates (point types, auto mode, tools, booleans, compounds) <!-- Vector rows rewritten to the new reality: point types + `auto`, the pen/node vocabulary, booleans and compounds folded into the "Vector paths" row; `compound` added to the node-kinds row; the "Nothing renders a path" bullet retired and replaced with the editing + production-render-channel bullets (the packet's host-composed `pathCommands`/`overlayCommands` channels, ADR 0014); finding 5's path stop-gap rewritten honestly — the loud `SCENE_ADAPTER_UNSUPPORTED_KIND:path` failure is deliberately replaced by opacity-0 projection, so save/reload now SILENTLY degrades path/compound geometry until the Scene persistence round-trip is retired; the schema section notes `compound` joined the v3 kind set additively (no bump, no migration); test-count table re-measured by running the suites: editor-kernel 241, scene-renderer 52, scene-renderer-wasm 51 vitest + 36 cargo, crafty-web 82. The documented "99 tests" for scene-renderer was wrong even at glass-fills close-out (measured 52); cli 14 / pen-import 13 / scene-store 41 were also measured and left for their own changes' close-outs. -->
- [x] 6.3 Full verification: `npm run typecheck`, `npm test`, `npm run lint`, `npm run format:check`, `npm run build` (rebuild changed packages first — the stale-`dist` rule) <!-- Run 2026-08-08, in order: package rebuilds (editor-kernel, scene-renderer, scene-renderer-wasm incl. cargo + wasm-bindgen) green; root typecheck 28/28; `npm test` — canvas lineage green: editor-kernel 241, scene-renderer 52, scene-renderer-wasm 51 vitest (1 skipped) + 36 cargo, crafty-web 82. The block-compiler lineage ran FULLY GREEN in this tree — the ~59 pre-existing failures the earlier agents verified against a clean tree did not reproduce here (reported, not silently dismissed). One flake: `protocol-v2-batch.test.ts`'s 3x speedup benchmark failed under turbo load (1.68x) and passed in isolation (3.54x) — a timing benchmark, not a regression. `npm run lint` clean; `npm run format:check` flags only pre-existing/other-change files (`files/[slug]/layout.tsx`, `inspector.tsx` — reported earlier — plus `app/layout.tsx` and `themes.tsx` from the next-server/chrome change), untouched; root `npm run build` 28/28. The renderer.md reality update rides this tick (the packet's path commands now reach the module in production through the host-composed `pathCommands` channel — previously headless-only, since the adapter threw before any production frame could carry a path command; protocol version stays 4). -->
