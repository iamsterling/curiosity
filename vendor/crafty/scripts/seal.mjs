import fs from 'node:fs'; import path from 'node:path'; import crypto from 'node:crypto'; import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'); const target=process.argv[2]||'/private/var/folders/wr/8bsbkjgd46v11_cpt9rfqm2r0000gn/T/opencode/opencode2-stage0-root.json'; if(fs.existsSync(target)) throw new Error('ROOT_ANCHOR_OVERWRITE_REFUSED'); const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex'); const record={schemaVersion:1,createdBy:'dedicated-seal-command',immutableByProtocol:true,notSignerIdentity:true,manifestDigests:{harness:sha(path.join(root,'freeze-manifest.json')),development:sha(path.join(root,'corpus/development/freeze-manifest.json')),holdout:sha(path.join(root,'corpus/holdout/freeze-manifest.json'))}}; fs.writeFileSync(target,JSON.stringify(record,null,2)+'\n',{flag:'wx'}); process.stdout.write(`SEALED ${target}\n`);
