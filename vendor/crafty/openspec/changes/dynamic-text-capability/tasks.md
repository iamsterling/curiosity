## 1. Change and Dependency Gates

- [x] 1.1 Reconcile this change against the then-current status and contracts of `authored-layout`, `vector-path-rendering`, `declarative-scene-api`, `canvas-actions-parity-tranche-2`, `universal-point-editing`, and `cms-foundation`; record any ownership conflict as an OpenSpec update before implementation.
- [x] 1.2 Confirm ADR 0020 and protocol v5 remain the implementation baseline; if source or an accepted ADR has moved, update this change before product work rather than translating stale assumptions.
- [x] 1.3 Define per-slice red/green evidence files and commands: each behavior test must fail for the intended reason before implementation, pass afterward without weakening the test, and preserve raw output for the gate review.
- [x] 1.4 Obtain explicit approval for the prerequisite and evidence-harness slices only; the v4→v5 schema/migration prerequisite is approved by [ADR 0023](../../../docs/architecture/adrs/0023-canonical-dynamic-text-content.md), and the bounded embedded-Inter contour evidence slice is approved by ADR 0024. Neither decision nor approval of this umbrella change approves deferred engine, font, range, WASM-resolution, or final-realization decisions.

## 2. Current-Substrate Prerequisites

- [x] 2.1 Add failing JSON document-deserialization table tests for number, boolean, null, array, plain object, and a valid `GlassFill`-shaped object as present text. For each, assert the exact one-element code sequence [`DOCUMENT_TEXT_VALUE_INVALID`], no accepted document, unchanged input bytes, and no normalization. Add string-on-invalid-kind coverage expecting exactly [`DOCUMENT_TEXT_KIND_INVALID`], and combined JSON-non-string-plus-invalid-kind coverage expecting exactly [`DOCUMENT_TEXT_VALUE_INVALID`] at every known source schema and the current schema. For each v1–v4 wrong-kind or non-string source fixture, prove rejection occurs before that source's migration step and that wrong-kind text is never stripped, relocated, or normalized. Do not add non-JSON values to deserialization tests.
- [x] 2.2 Add compile-time command tests proving the `text` property accepts only string. Add runtime-bypass command/kernel tests proving every value where `typeof value !== "string"` throws exactly `DOCUMENT_TEXT_VALUE_INVALID`; cover all JSON classes and representative `undefined`, function, symbol, and bigint values. Assert this code precedes `DOCUMENT_PROPERTY_VALUE_INVALID:text` and kind checking, including combined invalid-value/invalid-kind cases; assert unchanged canonical authored bytes, kernel revision, undo depth, and redo depth for every rejection. Retain valid whole-string exact inverse/undo/redo and honest no-op coverage.
- [x] 2.3 Add failing migration and persistence tests for the explicit `v4-to-v5-require-text-content` step: qualifying v4 text nodes with absent text become required `text: ""`; correctly-kinded present strings are preserved; present non-string values and wrong-kind strings are rejected before migration; a current-v5 text node with absent text returns exactly [`DOCUMENT_TEXT_VALUE_INVALID`]; the first replacement inverse/undo restores `""`, not absence; v1–v5 and v4–v5 applied-id sequences are exact; the existing v3→v4 migration targets an explicit v4 constant rather than the moving current-version constant and sequence evidence proves v3→v4→v5 is not skipped; v5 save/reopen canonical bytes and updated fixtures are stable; and schema 99 still returns `DOCUMENT_UNSUPPORTED_SCHEMA` with no migration applied.
- [x] 2.4 Implement the smallest ADR-approved type, validation, command, schema-v5, and v4→v5 migration correction that makes 2.1–2.3 green without introducing richer text schema or renderer behavior. Text value validation must precede text kind validation and the generic property-object guard; every known source-version validator must reject present non-string and wrong-kind text before its migration step; wrong-kind text must never be stripped, relocated, or normalized; current validation must require strings on text-capable nodes and prohibit text elsewhere; and v3→v4 must use an explicit v4 constant.
- [x] 2.5 Add the failing [ADR 0024](../../../docs/architecture/adrs/0024-correct-embedded-inter-contours.md) oracle: independently collect the raw embedded-Inter face-0 callback contours for `O`, `B`, and `8`, retaining each segment's ordinal, kind, endpoint, controls, and terminal `close`. Without sharing production normalization, derive canonical cubic tuples (line endpoint controls, exact quadratic degree elevation, unchanged cubic controls), then reconstruct production geometry as each consecutive stored-anchor segment followed by exactly one last-to-first wrap. Compare raw segment `Si` to reconstructed segment `i`, including source-kind handle presence and zero-delta curve controls; assert `n` raw segments become exactly `n` stored anchors and `n` reconstructed segments, with the terminal source segment assigned to the wrap, no duplicate first anchor, no closure-only synthetic segment, and matching contour count, transformed winding, and nonzero-hole behavior. Exercise line, quadratic, cubic, repeated-coordinate/source-zero-length, and rejected unrepresentable one-segment or malformed-close cases. Then run the declared size/affine matrix as an environment-qualified, exact-RGBA production-versus-source-reference comparison on a real WebGPU browser. A retained-point count, screenshot without the readiness signal, or invented pixel tolerance does not pass.
- [x] 2.6 Implement the bounded adapter correction approved by [ADR 0024](../../../docs/architecture/adrs/0024-correct-embedded-inter-contours.md): for raw `move_to(A0), S1 ... Sn, close`, require `n >= 2` and `Sn.end = A0`; store `A0` plus only the endpoints of `S1 ... S(n-1)`, never a duplicate terminal `A0`, and make `Sn` the closed-subpath last-to-first wrap. Map line sides to absent handles, exact quadratic elevation and cubic controls to independent `Free` outgoing/incoming handles (including present zero deltas), preserve `A0`'s outgoing control from `S1` while transferring `Sn`'s incoming control to `A0`, retain callback-sortable contour order and source degenerate segments, and reject unrepresentable/malformed contours without partial or synthetic geometry. Keep protocol-v5 text input and nonzero fill unchanged, and prove task 2.5 green without claiming shaping, layout, engine selection, final glyph realization, or full text fidelity.
- [x] 2.7 Run focused kernel, serialization, migration, scene-store fixture, renderer, and real-browser checks; attach raw red/green output, verify rejected input never reaches packet decode, and record the exact v4→v5 applied migration id and canonical-byte fixture deltas.

  Evidence: [`evidence/schema-v5-red-green.md`](evidence/schema-v5-red-green.md), the unchanged retained [`evidence/adr-0024-contour-acceptance-oracle.rs`](evidence/adr-0024-contour-acceptance-oracle.rs) with raw [`red`](evidence/adr-0024-contour-red.txt)/[`green`](evidence/adr-0024-contour-green.txt) output, [`evidence/adr-0024-cargo.txt`](evidence/adr-0024-cargo.txt), [`evidence/adr-0024-pixel-oracle-mutation.txt`](evidence/adr-0024-pixel-oracle-mutation.txt), [`evidence/adr-0024-vello-pixel-oracle-2026-08-16.json`](evidence/adr-0024-vello-pixel-oracle-2026-08-16.json), [`evidence/adr-0024-source-snapshot-manifest.json`](evidence/adr-0024-source-snapshot-manifest.json), [`evidence/2.7-canonical-byte-deltas.txt`](evidence/2.7-canonical-byte-deltas.txt) with raw implementation-linked [`canonical provenance`](evidence/2.7-canonical-provenance.txt), [`evidence/adr-0024-production-boundary.txt`](evidence/adr-0024-production-boundary.txt), and the `2.7-*` focused/repository gate logs in this directory. The independent synthetic callback oracle now compares line, quadratic, cubic, repeated-coordinate, zero-length, one-segment, and malformed-close classes against production collection/normalization. The real-browser matrix rerun from source archive SHA-256 `45a390398105668919ce3416837d0402918157ebbe4ed219d8522dd5e5376e16` passed all 36 rows with zero differing RGBA bytes and zero maximum delta; a one-pixel reference-capture mutation failed with exit code 2.

## 3. Executable Browser/WASM Evidence Harness

- [ ] 3.1 Obtain security-owner approval for an abuse-harness containment plan with enforceable process termination and explicit harness-specific CPU, wall-time, memory, upload, and GPU-resource ceilings; prove the harness terminates a benign ceiling-exceeding control without loading any malformed or oversized font. These containment ceilings are investigation safeguards, not production budgets, and no malformed or oversized font bytes may be executed before this task passes. **Post-cap correction remains blocked:** live inner admission now rejects bridge networking, writable root, extra tmpfs/bind/device exposure, writable cgroup-v2 mount metadata, unexpected identity/capability/environment/limit state, and any mount inventory drift. The current Docker Desktop `--network=none` namespace nevertheless exposes non-loopback tunnel interfaces, so the exact benign adversarial-admission criterion fails closed. See [`font-harness-threat-model.md`](font-harness-threat-model.md) and [`evidence/3.1-post-cap-correction.md`](evidence/3.1-post-cap-correction.md). No third independent review occurred. This does not execute or complete task 3.6.
- [ ] 3.2 Commit one licensed, pinned corpus covering mandatory Latin shaping, combining marks, emoji/ZWJ, Arabic/Hebrew mixed direction, Indic, CJK, whitespace, line breaking, missing glyph behavior, and variations; mark any color or bitmap glyph class optional only through an explicit capability exclusion and named unsupported diagnostic.
- [x] 3.3 Define implementation-neutral recorded outputs for logical ranges, clusters/affinity, lines, positioned glyphs, font identities, diagnostics, realization output, and environment-qualified browser pixels. **Post-review correction complete:** every identity-bearing array is pre-indexed with isolated duplicate diagnostics, canonical JSON strings use the specified scalar encoder rather than host `JSON.stringify`, and the committed adversarial vector's exact UTF-8 bytes/SHA-256 match a test-only Rust reference. Raw focused, repository, strict OpenSpec, production-boundary, and retained post-cap evidence is recorded in [`evidence/3.3-post-review-correction.md`](evidence/3.3-post-review-correction.md). No third independent review is available. Task 4.2 retains durable range-unit, normalization, grapheme, and affinity ownership.
- [ ] 3.4 Build and run a pinned Parley-family browser/WASM candidate adapter, recording build result, font-byte provisioning, mandatory corpus output, dependency/license inventory, and shipped artifact delta; a reproducible blocking diagnostic characterizes a rejected candidate and cannot qualify it for production.
- [ ] 3.5 Build and run a pinned cosmic-text-family browser/WASM candidate adapter against the identical contract and evidence fields; a reproducible blocking diagnostic characterizes a rejected candidate and cannot qualify it for production.
- [ ] 3.6 Only after 3.1 passes, run malformed and oversized font fixtures through every candidate parser/realizer path under the approved containment, recording enforced termination, CPU/memory/GPU-resource behavior, stable diagnostics, and authored/last-valid preservation; derive proposed production limits separately from evidence and security review. The approved boundary is the exact pinned Docker capsule from task 3.1; bare macOS still fails with `HARNESS_ADVERSARIAL_BOUNDARY_UNATTESTED`. This task has not been executed.
- [ ] 3.7 Compare repeated identical-input geometry separately from environment-qualified pixels and publish a gate report that distinguishes mandatory failures, explicitly optional glyph classes, conversion loss, API/version risks, and unresolved evidence.
- [ ] 3.8 Remove any harness shortcut that depends on production document schema, product protocol types, per-glyph crossings, or permanent dual-engine dispatch.

## 4. Contract, Security, OpenSpec, and ADR Decisions

- [ ] 4.1 Decide and specify one semantic owner and versioned input/output contract for authored content, font resolution, Unicode analysis/shaping, lines/clusters/affinity, measurement, hit testing, editing geometry, realization, and diagnostics.
- [ ] 4.2 Decide durable range units, normalization, grapheme behavior, bidi affinity, and all DOM/Rust/interchange conversion boundaries against non-BMP, combining, ZWJ, Indic, and mixed-direction evidence.
- [ ] 4.3 Decide font byte sources, byte/version identity, readiness, fallback/substitution/staleness, licensing/embedding boundaries, non-deterministic states, and native persistence implications.
- [ ] 4.4 Complete the external-font threat model and security review; approve fixture-backed parser/resource limits and failure ownership independently of provenance/license approval.
- [ ] 4.5 Specify with the `authored-layout` owner the fixed mapping for horizontal writing—horizontal/inline and vertical/block; `Fixed` supplies the declared resolved size as a text constraint, `Hug` consumes intrinsic text measurement, and `Fill` supplies the size distributed by authored layout—while leaving minimum/maximum and sizing ownership there; define text-to-layout dependency keys, bounded resolution/evaluator passes, stale-result handling, and full-resolution equivalence, and place any new text-content overflow field solely in `editor/dynamic-text-authoring` without redefining `Fixed | Hug | Fill`.
- [ ] 4.6 Select exactly one production layout engine, or stop: the selected candidate must pass every mandatory browser/WASM, Unicode, font-provisioning, geometry, security, dependency/license, and artifact criterion; blocking diagnostics reject candidates, and unsupported diagnostics may cover only glyph classes explicitly declared optional. Record exact pins, prototype-removal criteria, license/size impact, and the applicable ADR before integration; do not carry accepted mandatory gaps.
- [ ] 4.7 Decide the coarse resolution WASM boundary and resolved-text representation; obtain an ADR before adding an export or moving the Rust/TypeScript ownership line.
- [ ] 4.8 Update this OpenSpec change if any selected contract changes observable requirements or sequencing; strict validation must pass before the next slice begins.
- [ ] 4.9 Record the declarative-scene compatibility boundary: its existing plain-text-and-fill text remains on that change's existing protocol contract, and resolved-text integration is blocked pending a separate approved update dependent on the approved resolved-text packet contract and then-current renderer protocol version.

## 5. Read-Only Resolved Plain Text

- [ ] 5.1 Add failing conformance tests proving existing valid plain strings resolve into lines, clusters, glyphs, bounds, caret/affinity geometry, font identities, and diagnostics for the approved corpus while canonical persistence remains unchanged.
- [ ] 5.2 Add failing coherence tests proving intrinsic measurement, text-level hit queries, selection/caret geometry, and renderer projection consume the same resolution revision; retain node-box broad phase only.
- [ ] 5.3 Add failing invalidation and fault tests for content, width, font readiness/identity/fallback, language/direction, features/variations, stale results, malformed fonts, and resolver failure, with full resolution as oracle and last-valid preservation.
- [ ] 5.4 Integrate the selected resolver and bounded embedded/pinned font path only if every mandatory conformance, coherence, invalidation, fault, and security test is green; diagnostics cannot substitute for mandatory behavior, and unsupported diagnostics are limited to explicitly optional glyph classes. Do not add rich schema, arbitrary fonts, or editing.
- [ ] 5.5 If positioned glyphs or font resources require a protocol change, pause for the renderer OpenSpec/ADR gate, preserve one coarse per-frame crossing, and reject product semantics at the packet boundary.
- [ ] 5.6 Prove native save/reopen canonical equivalence, unknown-version rejection, renderer semantic neutrality, and no serialization of resolved geometry or diagnostics.
- [ ] 5.7 Add a boundary test proving existing declarative-scene text remains only its bounded compatibility passthrough and cannot receive resolved-text semantics until its separate packet/protocol dependency update is approved.

## 6. Kernel-Owned Plain-Text Editing

- [ ] 6.1 Add failing kernel tests for insert/delete/replace, exact inverses, no-op honesty, human/agent parity, declared typing-history outcome, and undo/redo excluding ephemeral caret/composition state.
- [ ] 6.2 Add failing interaction tests for composition start/update/commit/cancel, grapheme deletion, bidi affinity, range selection, shortcut arbitration, and point-to-caret through the authoritative kernel coordinate/hit-test path.
- [ ] 6.3 Approve an ADR if command granularity, transactions, coalescing, history, or durable selection semantics change; update the capability spec before implementing changed observable behavior.
- [ ] 6.4 Add the smallest closed text-edit effect vocabulary and kernel command surface that makes the headless interaction tests green; DOM/React and candidate editor buffers remain adapters only.
- [ ] 6.5 Add platform-qualified browser input/IME and clipboard evidence for the declared first support matrix, including cancellation and replacement cases, without promoting untested platforms to supported status.
- [ ] 6.6 Audit all mutation paths and prove no DOM state, React state, renderer state, Rust editor buffer, CMS adapter, or agent-only path can commit durable text outside validated commands.

## 7. Authored Typography and Layout Integration

- [ ] 7.1 Add failing schema, migration, validation, canonical-serialization, clipboard, component-override, and unknown-version tests for the smallest approved typography, font-request, language/direction, feature/variation, and container intent.
- [ ] 7.2 Obtain the authored/resolved, schema/range, font persistence, and layout ownership ADR approvals required by the selected contract before changing the document schema.
- [ ] 7.3 Implement the deterministic migration from existing correctly-kinded plain strings without persisting shaped output, fallback results, discovered fonts, carets, or cache state.
- [ ] 7.4 Add failing per-axis fixtures for `Fixed`, `Hug`, and `Fill`, constrained Hug sizing, authored text-content overflow, and dynamic content, proving the same resolved lines feed authored-layout measurement, selection, hit/caret geometry, and rendering without changing authored sizing ownership.
- [ ] 7.5 Integrate text measurement with the existing versioned layout boundary and make content/width/font/context invalidation green against full-resolution equivalence and last-valid fallback.
- [ ] 7.6 Reconcile dynamic CMS or component content through the same validated/resolution path; prove those systems supply content/reference intent but do not own text layout, editing, or renderer semantics.

## 8. Glyph Realization Decision and Integration

- [ ] 8.1 Compare a Vello/vector hypothesis and a Glyphon-style atlas hypothesis using identical positioned-glyph/font inputs, topology/pixel fixtures, zoom/transforms, small sizes, color/bitmap cases, device loss, failure injection, and environment-recorded resource distributions.
- [ ] 8.2 Select exactly one ordinary production realization path, or stop, through a renderer ADR; selection requires every mandatory topology, pixel, transform, zoom, size, resource, fault, device-loss, and security case to pass, while blocking diagnostics reject candidates and unsupported diagnostics are limited to explicitly optional color/bitmap glyph classes. State which ADR 0020 clauses are extended, retained, narrowed, or superseded and define removal criteria for all investigation paths.
- [ ] 8.3 If a bounded secondary representation is required for a proven unsupported glyph class, specify its class, selection rule, cache/failure policy, and evidence in the ADR; do not create a general dual-realizer abstraction.
- [ ] 8.4 Add failing protocol/presentation tests for coarse submission, unknown versions, semantic neutrality, realization/allocation failure, font removal, device loss, and no partial presentation.
- [ ] 8.5 Integrate the selected realization until every mandatory contour/pixel fidelity, last-valid presentation, resource/security, and full-rebuild equivalence test is green; for each explicitly optional color/bitmap glyph class, prove its named unsupported diagnostic and absence of partial or substituted presentation.

## 9. Scoped Rich Intent and Interchange (Later Milestone)

- [ ] 9.1 Re-open this slice only after plain editing, range conversion, font resolution, and container conformance gates pass; otherwise leave rich authoring unavailable.
- [ ] 9.2 Propose and approve a separate schema/command ADR for inline and paragraph intent, normalization, component overrides, paste, undo, and migration.
- [ ] 9.3 Add failing split/merge/style/paste/override/undo fixtures proving total canonical normalization and valid non-BMP, combining, ZWJ, grapheme, and paragraph boundaries.
- [ ] 9.4 Implement scoped intent only after those fixtures fail for the intended reason, then make them green through validated invertible commands.
- [ ] 9.5 Define each supported native or external interchange target independently as logical-, editable-, and/or pixel-fidelity; add failing round-trip and unsupported-feature diagnostic fixtures before implementation.
- [ ] 9.6 Keep CMS structured content mapping and accessibility projection in separately approved scopes; do not infer either from rich-text storage.

## 10. Measured Optimization and Closure

- [ ] 10.1 Establish representative documents and mutation traces from the accepted prior slices; record environment and cold/warm font/cache states and distributions for layout, realization/upload, paint, memory, and eviction.
- [ ] 10.2 Propose cache keys, invalidation, eviction, atlas-full or realization degradation, and any numeric budget only from recorded measurements and through the applicable decision gate.
- [ ] 10.3 Add failing cache-equivalence tests for every geometry/bitmap-affecting input, font removal, policy change, and device loss; make them green while keeping GPU/cache identifiers disposable.
- [ ] 10.4 Run focused and repository-wide typecheck, tests, lint, format check, build where required, strict OpenSpec validation, browser/WASM corpus, and security fixtures; attach raw output and distinguish pre-existing failures.
- [ ] 10.5 Perform adversarial review against every SHALL scenario, invariants, dependency ownership, ADR conditions, prototype-removal criteria, and unsupported-scope statement; remediate findings without weakening tests.
- [ ] 10.6 Update architecture docs and research ledger only for implemented reality and accepted decisions, then sync/archive the OpenSpec change only when all implemented milestone tasks and evidence gates are complete.
